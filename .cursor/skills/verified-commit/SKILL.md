---
name: verified-commit
description: Creates a task-scoped commit only after validator and exact-tree postflight pass.
---

# Verified Commit

Use `scripts/commit_task.py`. Do not call `git add -A`, direct `git commit`, force push, or mix unrelated work.
