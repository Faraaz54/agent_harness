---
description: Inspect hook/audit health when hooks are installed for the target agentic IDE.
---

Run when hook support is enabled in a project:

```bash
python -B scripts/hook_health.py
```

Hooks are defence-in-depth only. Build, review, validation, commit, and delivery scripts remain authoritative.
