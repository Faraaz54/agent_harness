---
description: Compatibility command for legacy plan/spec workflows; converts traditional planning material into IDSD Intent and Expectations.
---

Prefer `/capture-intent` for new work. Use this command only when a project starts from a traditional specification.

Invoke `project-specification-compat`, then produce IDSD artifacts rather than treating the legacy spec as final implementation authority.
