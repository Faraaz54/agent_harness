# Python Pipeline Context

Use simple Python modules with deterministic tests. Prefer local fixtures over live external calls during build-auto. Pipeline tasks should preserve input-to-output traceability and include unit, integration, and local E2E evidence when stateful data flow is touched.
